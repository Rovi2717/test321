name: Simple Input
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Your name?'

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello ${{ github.event.inputs.name }}!"
